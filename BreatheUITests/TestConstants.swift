import Foundation

/// Constants used in UI tests
enum TestConstants {
    /// Environment variable key to clear all exercises when running UI tests
    static let clearExercises = "UI Tests: Clear Exercises"
}
